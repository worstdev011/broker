# =============================================================================
# Frontend Environment Variables
# Copy this file to .env.local and adjust values for your environment.
# =============================================================================

# -----------------------------------------------------------------------------
# Backend API (Server-side proxy)
# -----------------------------------------------------------------------------
# URL where Next.js proxies API requests (server-side rewrites).
# Leave empty for same-origin (cookies work via rewrites).
# Default in dev: http://localhost:3001
#
# API_BACKEND_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Public API URL (Client-side)
# -----------------------------------------------------------------------------
# Optional. Used for client-side direct API calls when needed.
# If not set, requests typically go same-origin and are proxied.
#
# NEXT_PUBLIC_API_URL=

# -----------------------------------------------------------------------------
# Support Channel (Optional)
# -----------------------------------------------------------------------------
# URL to support/help channel (e.g. Telegram). Shown in UI if set.
#
# NEXT_PUBLIC_SUPPORT_CHANNEL_URL=https://t.me/comfortrade_support
